---
- hosts: all
  become: yes
  tasks:
  - name: install git apt install git
    apt: 
      name: git
      state: present

  - name: install wget apt install wget
    apt:
      name: wget
      state: present

  - name: install tree apt install tree
    apt:
      name: tree
      state: present        